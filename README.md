# Warren
A resilient, multi-source subdomain discovery framework. Passive sources, active brute-force, permutation generation, and live-host probing in one pipeline, where any single source failing never takes down the run.
